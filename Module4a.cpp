module;
#include <iostream>
#include <cmath>

module student_1bib21046.Lab2.Variant9.Task4:a;


namespace RBPO::lab2::variant9::task4
{
	double funa(int i)
		{
			double a = (pow(-1, i) * (i + 1) / (pow(2, i - 1)));
			return a;
		}
}